apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appcenter.fullname" . }}-test-connection"
  labels:
{{ include "appcenter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "appcenter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
